name: frontend
description: A new Flutter project for Public Opinion Analysis.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.1.1
  fl_chart: ^0.66.0
  http: ^1.2.0
  url_launcher: ^6.2.5
  intl: ^0.19.0
  webview_flutter: ^4.7.0
  webview_windows: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
