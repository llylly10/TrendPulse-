# 多源舆情分析系统

一个基于 AI 的多平台舆情监测与分析系统，支持从 Reddit、YouTube、Twitter 采集数据，使用 GPT 进行智能分析，并通过 Flutter Web 前端展示可视化结果。

## 项目特点

- 🌐 **多平台采集**：支持 Reddit、YouTube、Twitter 三大平台
- 🤖 **AI 智能分析**：使用 GPT 进行情感分析和观点提取
- 📊 **可视化展示**：思维导图、情感分布、核心观点等
- 🔄 **定时任务**：支持关键词订阅和自动采集
- 🎨 **主题切换**：支持亮色/暗色/系统主题
- 🔍 **关键词隔离**：每个关键词的数据完全独立

## 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Flutter Web 前端                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 仪表盘   │  │ 源数据流 │  │ 定时任务 │  │ 主题设置 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↕ HTTP/JSON
┌─────────────────────────────────────────────────────────────┐
│                      FastAPI 后端服务                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 采集API  │  │ 分析API  │  │ 订阅API  │  │ 报警API  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────┐
│                        数据处理层                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                  │
│  │ 数据采集 │→ │ 数据清洗 │→ │ AI分析   │                  │
│  │collect.py│  │cleaning  │  │analysis  │                  │
│  └──────────┘  └──────────┘  └──────────┘                  │
└─────────────────────────────────────────────────────────────┘
                            ↕
┌─────────────────────────────────────────────────────────────┐
│                      数据存储层                               │
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │  SQLite 数据库   │  │  JSON 分析报告   │                │
│  │ multi_source.db  │  │ analysis_*.json  │                │
│  └──────────────────┘  └──────────────────┘                │
└─────────────────────────────────────────────────────────────┘
```

## 技术栈

### 后端
- **Python 3.10+**
- **FastAPI** - 高性能 Web 框架
- **SQLite** - 轻量级数据库
- **OpenAI API** - GPT 模型调用
- **APScheduler** - 定时任务调度
- **BeautifulSoup4** - HTML 解析
- **Pandas** - 数据处理

### 前端
- **Flutter 3.x** - 跨平台 UI 框架
- **Provider** - 状态管理
- **HTTP** - 网络请求
- **Flutter Web** - Web 部署

### 数据采集
- **youtube-search-python** - YouTube 搜索
- **youtube-transcript-api** - 字幕获取
- **Nitter** - Twitter 镜像站
- **Selenium** - 浏览器自动化（备选）

## 本地运行步骤

### 1. 环境准备

#### 安装 Python 依赖

```bash
# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

#### 安装 Flutter

1. 下载 Flutter SDK: https://flutter.dev/docs/get-started/install
2. 配置环境变量
3. 运行 `flutter doctor` 检查环境

### 2. 配置环境变量

创建 `.env` 文件：

```env
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
```

### 3. 初始化数据库

```bash
# 运行数据库迁移（如果需要）
python migrate_add_keyword.py
```

### 4. 启动后端服务

```bash
python api.py
```

后端服务将在 `http://localhost:8888` 启动。

### 5. 启动前端

```bash
cd frontend
flutter pub get
flutter run -d chrome
```

前端将在浏览器中打开。

### 6. 访问系统

打开浏览器访问 `http://localhost:xxxx`（Flutter 会显示端口号）

## API 文档

### 1. 采集数据

**POST** `/api/collect`

启动数据采集任务。

**请求体：**
```json
{
  "keyword": "Python",
  "language": "zh",
  "reddit_limit": 30,
  "youtube_limit": 30,
  "twitter_limit": 30
}
```

**响应：**
```json
{
  "status": "started",
  "message": "Collection task started"
}
```

### 2. 获取仪表盘数据

**GET** `/api/dashboard`

获取分析结果和统计数据。

**响应：**
```json
{
  "heat_index": 1234.5,
  "total_posts": 378,
  "sentiment": {
    "score": 65.5,
    "label": "正面"
  },
  "key_points": [
    "观点1",
    "观点2",
    "观点3"
  ],
  "summary": "AI 深度摘要...",
  "mermaid_graph": "graph TD; A[Python] --> B[优点]; ...",
  "node_sentiments": {
    "B": "positive",
    "C": "negative"
  },
  "keyword": "Python"
}
```

### 3. 获取源数据

**GET** `/api/source-data`

获取原始采集数据。

**响应：**
```json
[
  {
    "platform": "youtube",
    "content": "视频标题和内容",
    "author": "频道名称",
    "timestamp": "2026-01-16T10:00:00",
    "engagement": {
      "view_count": 10000
    },
    "url": "https://youtube.com/...",
    "keyword": "Python"
  }
]
```

### 4. 创建订阅

**POST** `/api/subscriptions`

创建定时采集任务。

**请求体：**
```json
{
  "keyword": "Python",
  "language": "zh",
  "reddit_limit": 30,
  "youtube_limit": 30,
  "twitter_limit": 30,
  "interval_seconds": 21600
}
```

**响应：**
```json
{
  "id": 1,
  "keyword": "Python",
  "interval_seconds": 21600,
  "next_run": 1737012345
}
```

### 5. 获取订阅列表

**GET** `/api/subscriptions`

获取所有订阅任务。

**响应：**
```json
[
  {
    "id": 1,
    "keyword": "Python",
    "language": "zh",
    "reddit_limit": 30,
    "youtube_limit": 30,
    "twitter_limit": 30,
    "interval_seconds": 21600,
    "last_run": 1737012345,
    "next_run": 1737033945
  }
]
```

### 6. 删除订阅

**DELETE** `/api/subscriptions/{id}`

删除指定订阅任务。

**响应：**
```json
{
  "status": "deleted"
}
```

### 7. 获取任务状态

**GET** `/api/task-status`

获取当前任务执行状态。

**响应：**
```json
{
  "is_running": true,
  "current_task": "collect",
  "progress": "数据采集中...",
  "last_update": 1737012345
}
```

### 8. 获取报警信息

**GET** `/api/alerts`

获取负面舆情报警。

**响应：**
```json
[
  {
    "id": 1,
    "subscription_id": 1,
    "message": "⚠️ 负面舆情报警: 'Python' 情感得分仅 25.3！",
    "created_at": 1737012345
  }
]
```

### 9. 清空数据

**POST** `/api/clear-data`

清空所有采集数据和分析报告。

**响应：**
```json
{
  "status": "cleared",
  "message": "All data cleared"
}
```

## 数据库结构

### crawl_task - 采集任务表
```sql
CREATE TABLE crawl_task (
    task_id INTEGER PRIMARY KEY AUTOINCREMENT,
    source_type TEXT,
    keyword TEXT,
    language TEXT,
    limit_count INTEGER,
    created_at INTEGER
);
```

### cleaned_data - 清洗后数据表
```sql
CREATE TABLE cleaned_data (
    platform TEXT,
    raw_id TEXT,
    content TEXT,
    author TEXT,
    timestamp TEXT,
    engagement TEXT,
    url TEXT,
    keyword TEXT
);
```

### subscriptions - 订阅任务表
```sql
CREATE TABLE subscriptions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    keyword TEXT,
    language TEXT,
    reddit_limit INTEGER,
    youtube_limit INTEGER,
    twitter_limit INTEGER,
    interval_seconds INTEGER,
    last_run INTEGER,
    next_run INTEGER
);
```

### alerts - 报警记录表
```sql
CREATE TABLE alerts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    subscription_id INTEGER,
    message TEXT,
    created_at INTEGER
);
```

## 项目结构

```
.
├── api.py                      # FastAPI 后端服务
├── collect.py                  # 数据采集模块
├── data_cleaning.py            # 数据清洗模块
├── ai_analysis.py              # AI 分析模块
├── requirements.txt            # Python 依赖
├── .env                        # 环境变量配置
├── multi_source.db             # SQLite 数据库
├── analysis_report_*.json      # 分析报告文件
│
├── frontend/                   # Flutter 前端
│   ├── lib/
│   │   ├── main.dart          # 应用入口
│   │   ├── models/            # 数据模型
│   │   ├── pages/             # 页面组件
│   │   ├── providers/         # 状态管理
│   │   ├── services/          # API 服务
│   │   ├── widgets/           # UI 组件
│   │   └── utils/             # 工具函数
│   ├── pubspec.yaml           # Flutter 依赖
│   └── web/                   # Web 配置
│
└── docs/                       # 文档目录
    ├── README.md              # 本文档
    ├── ANTI_SCRAPING.md       # 反爬策略
    └── TECHNICAL.md           # 技术文档
```

## 使用指南

### 1. 采集数据

1. 在仪表盘点击右上角 "+" 按钮
2. 输入关键词（例如：Python）
3. 选择语言和采集数量
4. 点击"开始采集"
5. 等待任务完成（底部会显示进度）

### 2. 查看分析结果

- **仪表盘**：查看热度指数、情感分析、思维导图、核心观点、AI 摘要
- **源数据流**：查看原始采集数据
- **定时任务**：管理订阅任务

### 3. 创建定时任务

1. 切换到"定时任务"标签
2. 点击右上角 "+" 按钮
3. 输入关键词和时间间隔
4. 点击"创建订阅"
5. 系统将自动定时采集和分析

### 4. 主题切换

1. 点击仪表盘右上角三个点
2. 选择"主题设置"
3. 选择亮色/暗色/系统主题

## 常见问题

### Q1: 采集失败怎么办？

**A:** 检查：
1. 网络连接是否正常
2. API Key 是否配置正确
3. 查看后端日志了解具体错误

### Q2: 为什么有些平台采集不到数据？

**A:** 可能原因：
1. 关键词太冷门，没有相关内容
2. 平台限制或镜像站失效
3. 网络问题导致超时

### Q3: AI 分析需要多长时间？

**A:** 取决于数据量和 API 响应速度，通常 30-60 秒。

### Q4: 如何清空所有数据？

**A:** 在仪表盘点击右上角三个点 → "清空所有数据"

### Q5: 支持哪些语言？

**A:** 目前支持中文（zh）和英文（en）。

## 开发计划

- [ ] 支持更多平台（微博、知乎等）
- [ ] 添加数据导出功能
- [ ] 支持自定义 AI Prompt
- [ ] 添加用户认证系统
- [ ] 支持分布式采集
- [ ] 添加数据可视化图表

## 贡献指南

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License

## 联系方式

如有问题，请提交 Issue 或联系开发者。

---

**注意**：本项目仅用于学习和研究目的，请遵守相关平台的使用条款和法律法规。
